import PageHeader from "@/components/shared/PageHeader";

export default function EntradaXML() {
  return (
    <div className="space-y-4">
      <PageHeader title="Entrada XML" subtitle="Upload de XML da SEFAZ e preview (simulado)" />
      <div className="rounded-lg border p-4 text-sm text-muted-foreground">Formulário de upload e preview (mock).</div>
    </div>
  );
}
